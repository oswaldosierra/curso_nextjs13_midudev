export default function Layout({ children }) {
    return (
        <div>
            <marquee behavior="" direction="">hola que tal soy un layout</marquee>
            {children}
        </div>
    )
}
